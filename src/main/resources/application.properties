spring.application.name=app

# Default profile (if not specified, use 'dev')
# BlockHound will be ENABLED in dev/test profiles
# BlockHound will be DISABLED in prod/production profiles
spring.profiles.default=dev

# Logging Configuration
logging.level.com.loginservice.app.config.LogFilter=INFO
